<?xml version="1.0" encoding="UTF-8"?>
<!--
    An explicit status listener sends logback's own configuration chatter
    (the |-INFO / |-WARN status lines) to stderr, keeping the app's stdout a
    pure JSON Lines stream that can be piped directly into jlx or redirected
    to a file. Without any status listener, logback's LOGBACK-292 fallback
    would dump the entire configuration status list to stdout whenever the
    config produces a WARN or ERROR.
-->
<configuration scan="true" scanPeriod="30 seconds">

    <statusListener class="ch.qos.logback.core.status.OnErrorConsoleStatusListener"/>

    <!--
        JSON rolling file appender using JsonAppenderRolling + JsonLogWriter.
        Emits flat JSON fields: ts, level, logger, thread, msg, key/value pairs,
        and (for exceptions) errClass, errMessage, stack, errHash.
    -->
    <appender name="JSON_FILE" class="hr.hrg.dialog.logback.JsonAppenderRolling">
        <file>logs/example.jsonl</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!--
                Logback 1.5.18+ has native XZ compression support: when the
                fileNamePattern ends with .xz, rotated files are compressed with
                the org.tukaani:xz library using XZCompressionStrategy.
            -->
            <fileNamePattern>logs/example.%d{yyyy-MM-dd}.%i.jsonl.xz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
        <!-- No <encoder> needed: JsonAppenderRolling.writeOut() writes JSON
             directly and installs a no-op encoder itself. -->
    </appender>

    <root level="DEBUG">
        <appender-ref ref="JSON_FILE" />
    </root>

</configuration>